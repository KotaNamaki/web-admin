import { TrainerList } from './TrainerList';
import { UserEdit as TrainerEdit } from '../users/UserEdit';
import { UserCreate as TrainerCreate } from '../users/UserCreate';
import { Contact as TrainerIcon } from 'lucide-react';

export default {
    list: TrainerList,
    edit: TrainerEdit,
    create: TrainerCreate,
    icon: TrainerIcon,
};
